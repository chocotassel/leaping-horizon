# Neon Slice：结构化心流谱面设计

更新日期：2026-08-16

## 当前设计目标

目前只做好一个“心流”难度：全程需要注意，但压力应像音乐句子一样有呼吸、有重复、有高潮。玩家听到同一段旋律再次出现时，应能认出同一套操作；进入另一个乐句或小节时，也应看到明确的新组合，而不是连续不断、边界模糊的随机地刺。

`music-structure-template-v3` 因此把体验拆成三层：

1. **音乐结构**决定何时换段、哪些乐句属于同一身份；
2. **家族母版**决定同一旋律对应哪套固定障碍；
3. **心流强度**决定一个家族可用的形态词汇，以及高潮是否增加真实旋律 overlay。

强度不再拥有任意切段权。它不能让第二次出现的同一旋律临时换一套路线。

## 音乐语法的三个尺度

### Section：歌曲阶段

librosa 特征和 scikit-learn 多尺度聚类在真实 downbeat 上寻找换段。本曲得到 14 个 section；边界为：

```text
1.06, 9.34, 17.62, 34.16, 42.44, 58.98, 67.26,
92.06, 116.84, 125.10, 133.34, 141.58, 149.82, 158.04, 164.84 s
```

这些 section 用于展示歌曲大结构和定位明显换段。它们来自多个聚类尺度共同支持的 downbeat，不是按固定时长切开。

### Phrase family：重复乐句身份

主编排单位是 8 小节乐句。相似度同时看和声、音色和微节奏；同家族生成一次 canonical template，并在所有 occurrence 精确复用。

当前 10 个主乐句：

| 乐句 | 时间 | 分析家族 | 设计意义 |
| --- | --- | --- | --- |
| phrase-01 | 1.06–17.62 | FU01 | 纯方块开场，引导注意力进入 |
| phrase-02 | 17.62–34.16 | FU02 | 教学组合，包含标准 M 口袋 |
| phrase-03 | 34.16–50.72 | FA | 主旋律 A 第一次 |
| phrase-04 | 50.72–67.26 | FB | 主旋律 B 第一次 |
| phrase-05 | 67.26–83.80 | FU05 | 独立过渡与爬升 |
| phrase-06 | 83.80–100.32 | FA | 完整复用 phrase-03 |
| phrase-07 | 100.32–116.84 | FB | 完整复用 phrase-04 |
| phrase-08 | 116.84–133.34 | FU08 | 回落与重新聚焦 |
| phrase-09 | 133.34–149.82 | FU09 | 高强度预热；同时继承跨界重复片段 |
| phrase-10 | 149.82–164.84 | FU10 | 最终高潮与旋律 M |

FA 和 FB 合起来形成可识别的 16 小节句子：34.16–67.26 秒在 83.80–116.84 秒原样返回。另一个旋律从标准乐句的中间开始，因此还使用步长 4 小节的重叠窗口：42.44–58.98 秒在 92.06–108.58 和 133.34–149.82 秒再次出现。编译器会复制这些跨界范围，不让固定 8 小节边界切断旋律身份。

### Bar module：小节内的动作句法

每个 8 小节母版有八个角色：

| `barRole` | 体验作用 |
| --- | --- |
| `opening` | 建立当前乐句的起点和方向 |
| `call` | 提出动作主题 |
| `answer` | 回应或变换主题 |
| `turn` | 明确折返/转折 |
| `lift` | 抬高注意力 |
| `drive` | 保持连续操作 |
| `peak` | 乐句内部压力峰 |
| `cadence` | 收束并准备下一句 |

每个可玩小节都保存 `downbeatCue`，让试听室、调试工具和后续视觉效果可以明确显示小节起点。cue 附着在真实 Beat This! 槽位上，不会人工补一个事件。

强度先选择 C/S/M 等形态词汇，`familyId` 再确定词汇的旋转次序，所以不同家族得到不同的 bar motif sentence；同家族的次序、方向和五道障碍行保持一致。当前 FA 与 FB 的模块顺序已经不同，不会再因强度相近而长成同一段。

## 元形态词典

| 形态 | 操作意义 | 生存路线 / Combo 路线 |
| --- | --- | --- |
| 专注引导 `focus` | 只有方块，没有地刺 | 用于开场、收句与局部呼吸 |
| C 形 `c` | 地刺给出弧形方向 | 保留一侧露营边；追 Combo 才跟目标深入 |
| 长扫 `sweep` | 稳定从一侧滑向另一侧 | 宽走廊，持续移动但容易预判 |
| V 形 `v` | 横扫后折返 | 明确训练反向操作 |
| 呼吸门 `pulse` | 走廊交替张开和收紧 | 横移不大，站位要求更准确 |
| S 形 `s` | 跨跑道后连续反向 | 没有永久露营边，要求持续左右滑动 |
| Z 形 `zigzag` | 停顿、横切、再次折返 | 强调启停和反应 |
| M 形 `m` | 两堵地刺墙包住对侧连续方块 | 求生可留安全边；保 Combo 必须进出口袋 |

模板会把每个小节的首尾尽量收回中道，并按所有 occurrence 中最严格的真实时间间隔计算可移动道数。这样模板在第一次和再次出现时都可达，也能安全连接前后家族。

## 标准 M：固定风险—收益口袋

标准左口袋必须为六个真实 Beat This! 槽：

```text
22200, 00000, 10000, 10000, 00000, 22200
```

镜像定义为：

```text
00222, 00000, 00001, 00001, 00000, 00222
```

当前编译器使用左口袋。第一和第六行是纯躲避墙；第二、第五个 `00000` 是横移时间，不作为空事件写入；第三、第四行要求追求 Combo 的玩家主动左划吃方块，再撤回安全侧。phrase-02 用标准版教学，phrase-10 在 153.42 秒主高潮使用 melodic M，并只允许真实检测峰形成额外旋律。

## 方块密度与高潮

方块间隔不再服从“每 5–6 格一个”的平均规则：

- 314 个可玩 Beat This! 槽构成稳定节拍骨架；
- Basic Pitch、librosa onset 和 preference fusion 提供节拍之间的旋律峰；
- 重复家族只接纳每个 occurrence 都出现的同位置装饰，成为 `auxiliary-common`；
- 独立乐句可以保留更多真实装饰；
- 以 153.42 秒为中心的高潮可额外接纳 `overlay`，这是唯一允许 occurrence 局部变化的层；
- 所有加花仍要通过 Full Combo 路线验证，过密但不可达的候选会被拒绝。

当前主谱有 422 个击打方块、4 个纯躲避事件，共 426 个事件；其中 116 个为真实旋律加花。共 301 行带地刺、733 个地刺单元，最长短间隔旋律连段为 5。高潮的方块率和地刺压力都高于开场。

## “同一”与“变化”的边界

必须完全相同：

- 同家族 `core` 的相对槽、目标道和地刺行；
- 同家族 `auxiliary-common` 的相对位置和障碍行；
- FA、FB、组合后的 16 小节返回，以及 42.44 / 92.06 / 133.34 秒重叠旋律；
- 同一 canonical template 的方向变换和小节模块次序。

允许变化：

- 不同家族可以使用不同 motif sentence；
- `related-variant` 只表示听感相关，不自动复用；
- 高潮 `overlay` 可以根据该次真实检测峰与可达路线变化；
- occurrence 的绝对时间天然不同，始终保留各自 detector timestamp。

## 自动可玩性验收

自动验证不是只看“有没有空道”，而是同时求解：

- **生存路线**：可以放弃目标，但不能撞地刺；
- **Full Combo 路线**：每个方块必须命中，同时穿过纯躲避墙。

还会检查：

- 每次横移符合真实时间差和 `0.23 s/道` 的速度预算；
- 第 2–4 道不存在地刺夹出的单格空口；
- C 形保留一条安全边；
- 标准 M 六槽逐项匹配，两个空槽不写入事件；
- 每个可玩 bar module 都有 downbeat cue；
- 重复家族的一致率为 100%；
- 不同家族的 bar plan 不应意外坍缩为同一组合；
- 开场保持无地刺聚焦，高潮方块和地刺密度达到阈值；
- 元数据统计必须与 JSON 中实际事件完全相符。

## 试听时应重点判断什么

打开 `/rhythm-lab.html`：同家族用同色，白线是 section 边界，结构条和重复家族按钮都可直接跳转。

下一轮听感验收建议按以下顺序：

1. 在 34.16 和 83.80 秒之间来回跳，确认 FA 操作能被记住；
2. 在 50.72 和 100.32 秒之间比较 FB，并确认它和 FA 本身有明显区别；
3. 连续比较 42.44、92.06、133.34 秒，确认跨界旋律仍是同一组障碍；
4. 从 149.82 秒进入高潮，确认 M 和 overlay 增强压力，但不会破坏路线；
5. 若边界仍不对，优先调整结构特征、相似阈值或 bar plan，不要改成 BPM 网格或手工移动事件时间。
